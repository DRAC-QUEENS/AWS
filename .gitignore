# Terraform
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.*
terraform.tfvars
terraform.tfvars.json
crash.log
crash.*.log
tfplan
*.tfplan
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.env
.env.local

# Resumen personal para revision offline, no forma parte del repo final
INFRAESTRUCTURA.md

# Terraform state con cualquier nombre (no solo terraform.*)
*.tfstate
*.tfstate.backup
*.tfstate.lock.info

# user_data renderizados: solo se versionan las plantillas .tpl
user_data/*.sh
!user_data/*.sh.tpl

# Claves, certificados y SSH
*.pem
*.key
*.pfx
*.p12
id_rsa
id_ed25519
*.crt

# Credenciales y entornos varios
.envrc
credentials
.aws/

# Docs operativas que han contenido secretos en el pasado
WIREGUARD_BOOT_STATUS.md
WIREGUARD_SETUP.md
WIREGUARD_MANUAL.md
